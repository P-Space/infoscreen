### Blinds daemon service

A Python daemon for powering on/off the screen of raspberry-pi

### Requires

* Python (>2.6)
* Python sh module. Install it with `pip install sh`
* vcgencmd (builtin on raspbian)
